<UserControl x:Class="FlashFrancais.FlashDeckControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlashFrancais"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="308*"/>
            <RowDefinition Height="38*"/>
            <RowDefinition Height="69*"/>
        </Grid.RowDefinitions>
        <Label HorizontalAlignment="Center" Foreground="Green" Grid.Row="0" Content="Current Card" Margin="361,0,361,308.036" Grid.RowSpan="2"/>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Text="{Binding CurrentCardText, UpdateSourceTrigger=PropertyChanged}" FontSize="36"/>

        <Grid Grid.Row="1" Margin="0,275.333,0,0" Grid.RowSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding FlipCurrentCardCommand}" Content="Flip" FontSize="36" RenderTransformOrigin="1.75,-1.787" Margin="10,0,10,10"/>
            <Button Grid.Column="1" Command="{Binding GetNextCardCommand}" Content="Next" FontSize="36" Margin="10,0,10,10"/>
        </Grid>
    </Grid>
</UserControl>
